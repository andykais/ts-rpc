{
  "name": "ts-rpc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "develop": "pnpm run build:this && cd sample  && pnpm i ../build && pnpm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "typecheck:watch": "tsc --watch --noEmit",
    "compile:watch": "tsc --watch",
    "build": "rm -rf build && tsc && cp package.json build",
    "build:sample": "npm run build && cd sample && pnpm un ts-rpc && pnpm i && pnpm i ../build/ && npm run build",
    "start": "cd sample && npm run start"
  },
  "keywords": [],
  "author": "Andrew Kaiser",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.4.10",
    "typescript": "^3.9.5"
  }
}
