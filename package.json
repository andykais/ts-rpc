{
  "name": "ts-rpc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "develop": "pnpm run build:this && cd sample  && pnpm i ../build && pnpm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "typecheck:watch": "tsc --watch --noEmit",
    "compile:watch": "tsc --watch",
    "build:this": "rm -rf build && tsc && cp package.json build/package.json",
    "build": "npm run build:this && cd sample && npm un ts-rpc && npm i && npm i ./deps/ts-rpc-1.0.0.tgz && npm run build",
    "start": "cd sample && npm run start"
  },
  "keywords": [],
  "author": "Andrew Kaiser",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.4.10",
    "typescript": "^3.9.5"
  }
}
